<h2>New Tweet!</h2>
<form class="tweet-compose" data-tweets-ul="#feed" action="/tweets" method="POST">
  <input
      type="hidden"
      name="authenticity_token"
      value="<%= form_authenticity_token %>">

  <label>
    Content
    <textarea name="tweet[content]"></textarea>
  </label>
  <strong class="chars-left"></strong>
  <br>

<div class="mentioned-users">

  <label>
    Mention
    <a href="#" class="add-mentioned-user" onclick="return false;">Add Mention</a>
    <script type="text/template">
    <select name="tweet[mentioned_user_ids][]">
      <option></option>
      <% User.all.each do |user| %>
        <option value="<%= user.id %>"><%= user.username %></option>
      <% end %>
    </select>
  </script>
</label>
</div>

  <br>

  <input type="Submit" value="Post Tweet!">
</form>
